class AppLoginState{
  final String userId;
  final bool isOnBoardingDone;
  final bool isLoggedIn;
  final bool isRegistered;

  AppLoginState(this.userId, this.isOnBoardingDone, this.isLoggedIn, this.isRegistered);
}